<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Seu Título</title>
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
</head>
<body>
  <!-- Seu conteúdo HTML aqui -->
  
  <script>
    // Função para iniciar o SweetAlert e a atualização dos elementos do HTML
    async function iniciarSweetAlert() {
      try {
        // Criação do SweetAlert
        Swal.fire({
          title: 'Mod criado por @henrydeev_ 🇧🇷💖!',
          text: 'Esse mod de Amor Doce basicamente altera o valor das moedas e também dos PAs, use com moderação!',
          showCancelButton: true,
          confirmButtonColor: '#3085d6',
          cancelButtonColor: '#d33',
          confirmButtonText: 'Iniciar'
        }).then((result) => {
          if (result.isConfirmed) {
            // Inicia a atualização a cada segundo
            setInterval(function() {
              var elementos = document.getElementsByTagName('*');

              for (var i = 0; i < elementos.length; i++) {
                var elemento = elementos[i];

                if (elemento.innerHTML.includes('220')) {
                  elemento.innerHTML = elemento.innerHTML.replace(/220/g, '999999');
                }

                if (elemento.innerHTML.includes('505')) {
                  elemento.innerHTML = elemento.innerHTML.replace(/505/g, '999999');
                }
              }
            }, 1000);
          }
        });
      } catch (error) {
        console.error('Ocorreu um erro ao iniciar o SweetAlert2:', error);
      }
    }

    // Chama a função para iniciar o SweetAlert e a atualização dos elementos
    iniciarSweetAlert();
  </script>
</body>
</html>
